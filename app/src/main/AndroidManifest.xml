<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anudeepthi.feelgood">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RelaxOptions"
            android:label="@string/title_activity_relax_options"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Blog"
            android:label="@string/title_activity_blog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PostBlog"
            android:label="Write it out" />
        <activity
            android:name=".RelaxOptsContent"
            android:label="@string/title_activity_yoga"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Bunny" />
        <activity android:name=".MainFacts" />
        <activity android:name=".WebApp" />
        <activity android:name=".FillFormApp"></activity>
    </application>

</manifest>